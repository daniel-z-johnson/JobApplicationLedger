# Copy to .env.local to override the committed defaults on your machine.
# .env.local is ignored by Git. Never put secrets in VITE_ variables.
VITE_API_BASE_URL=/api
DEV_API_TARGET=http://localhost:8080
