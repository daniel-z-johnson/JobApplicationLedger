spring:
  application:
    name: ledger
  jpa:
    open-in-view: false
  ai:
    ollama:
        model: mistral:latest
server:
  servlet:
    context-path: /api

management:
  endpoints:
    web:
      exposure:
        include: "*"
