spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/joba
    username: joba
    password: postgress
  data:
    redis:
      host: localhost
      port: 6379

server:
  servlet:
    session:
      cookie:
        http-only: true
        secure: false
        same-site: lax
